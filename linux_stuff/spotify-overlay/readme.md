This Node.js script utilizes `playerctl` on a Linux machine in order to grab more information about the music currently playing through a Spotify instance.

The `web` folder contains an example overlay.